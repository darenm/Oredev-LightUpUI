<Page
    x:Class="CreatorsNews.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CreatorsNews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:CreatorsNews.ViewModels"
    xmlns:model="using:CreatorsNews.Models"
    xmlns:shared="using:Shared"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:appmodel="using:Windows.ApplicationModel"

    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="DefaultTemplate" x:DataType="model:MenuItem">
            <Grid Width="240" Height="48"
                  HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <SymbolIcon Grid.Column="0" Symbol="{x:Bind Icon, Mode=OneWay}" Foreground="Black" />
                <TextBlock Grid.Column="1" Text="{x:Bind Name, Mode=OneWay}" FontSize="16" VerticalAlignment="Center"
                           Foreground="Black" />
            </Grid>
        </DataTemplate>

        <AcrylicBrush x:Key="NavigationViewDefaultPaneBackground"
                      BackgroundSource="Backdrop" TintColor="Orange" TintOpacity=".6" />
        <AcrylicBrush x:Key="NavigationViewExpandedPaneBackground"
                      BackgroundSource="HostBackdrop" TintColor="Orange" TintOpacity=".8" />
    </Page.Resources>

    <!-- ApplicationPageBackgroundThemeBrush -->
    <!-- SystemControlAcrylicWindowBrush -->
    <Grid Background="{ThemeResource SystemControlAcrylicWindowBrush}">
        <!--<controls:HamburgerMenu x:Name="HamburgerMenuControl"
                                PaneBackground="Orange"
                                Foreground="Black"
                                DisplayMode="Inline"
                                IsPaneOpen="True"
                                ItemTemplate="{StaticResource DefaultTemplate}"
                                ItemClick="OnMenuItemClick"
                                OptionsItemTemplate="{StaticResource DefaultTemplate}"
                                OptionsItemClick="OnMenuItemClick">

            <Frame x:Name="RootFrame" />
        </controls:HamburgerMenu>-->

        <TextBlock x:Name="AppTitle" Style="{StaticResource CaptionTextBlockStyle}"
                   Text="{x:Bind appmodel:Package.Current.DisplayName}" IsHitTestVisible="False" Foreground="Red" />


        <NavigationView x:Name="NavView"
                        Background="{ThemeResource SystemControlAcrylicWindowBrush}"
                        SelectionChanged="NavView_SelectionChanged">

            <NavigationView.HeaderTemplate>
                <DataTemplate>
                </DataTemplate>
            </NavigationView.HeaderTemplate>

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Icon="Home" Tag="Home" />
                <NavigationViewItem Content="Games" Icon="XboxOneConsole" Tag="Games" />
                <NavigationViewItem Content="News" Icon="Document" Tag="News" />
            </NavigationView.MenuItems>

            <Frame x:Name="RootFrame" />

        </NavigationView>
    </Grid>
</Page>